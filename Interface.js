var SancCity = SancCity || {}

SancCity.interface = {};


// this send s a messaage to the user
SancCity.interfecae.notify = function (message, type){
    console.log(message +' - ' + type);
};

//  this will refresh the stats of our convoy
 Sancicty.interface.refreshConvoy = function(){
    console.log(this.convoy
    );
 }
